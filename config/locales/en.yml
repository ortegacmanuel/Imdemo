en:
  activerecord:
    models:
      client: Client
      document_type: Document type
    attributes:
      product:
        document_types: Required documents
      work_process:
        products: Client asked for
        header_products: Required products
